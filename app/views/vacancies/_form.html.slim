- if @vacancy.errors.any?
  .alert.alert-danger
    strong
      | Из-за #{@vacancy.errors.count} 
      | #{Russian.p(@vacancy.errors.count, "ошибки","ошибок","ошибок")} 
      | нельзя сохранить данные:
    ul
      - @vacancy.errors.full_messages.each do |message|
        li = message

= form_for @vacancy, html: {class:'form-horizontal'} do |f|
  
  .form-group
    label.col-sm-4.control-label Название:
    .col-sm-8
      = f.text_field :name, class: 'form-control', placeholder: 'Укажите название вакансии'

  .form-group
    label.col-sm-4.control-label Срок действия:
    .col-sm-8
      = f.text_field :valid_until, value: (f.object.valid_until.strftime('%d.%m.%Y') if f.object.valid_until), class: 'form-control', id: 'valid_until'

  .form-group
    label.col-sm-4.control-label Зарплата:
    .col-sm-8
      = f.text_field :salary, class: 'form-control', placeholder: 'Укажите уровень зарплаты'

  .form-group
    label.col-sm-4.control-label Контакты:
    .col-sm-8
      = f.text_field :contacts, class: 'form-control', placeholder: 'Укажите телефон или почту'

  .form-group
    label.col-sm-4.control-label Умения:
    .col-sm-8
      = f.text_field :skills_array, id: 'skill_select'

  .form-group
    .col-sm-8.col-sm-offset-4
      = button_tag type='submit', class: 'btn btn-default' do
        span.glyphicon.glyphicon-ok
        span.btn-label Сохранить


